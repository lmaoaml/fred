This repo contains an implementation of the Fusional Reduction algorithm (FRed) as described by Brasoveanu & Prince (2011).

The function FRed() takes as input a comparative tableau and returns the Maximally Informative Basis if a satisfying ranking can be assigned.
